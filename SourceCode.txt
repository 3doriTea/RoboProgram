void RunLong()
{
  Run();  // これを書くと走るよ
  Run();Run();Run();Run();Run();Run();Run();Run();Run();Run();Run();Run();
  Run();Run();Run();Run();Run();
}

// 毎ビート呼ばれるよ！
void Update()
{
  int tileNumber = GetOnTileNumber();
  int a = tileNumber + 10 * 10;

  // もし、tileの番号が1なら、ジャンプするよ
  if (tileNumber != 1)
  {
   Jump();  // 条件が満たされたとき、「Jump関数」が呼び出される
  }
  RunLong();
}
